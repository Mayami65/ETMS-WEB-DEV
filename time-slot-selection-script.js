function bookTimeSlot() {
    // Dummy booking logic (replace with actual booking logic)
    var selectedTimeSlot = document.getElementById('timeSlot').value;
    alert('Time slot booked: ' + selectedTimeSlot);
    // You can add further actions like showing a confirmation message or updating the UI.
}
